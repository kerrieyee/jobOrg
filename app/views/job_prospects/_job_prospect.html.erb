<tr id="<%=job_prospect.id%>" >
	<td><%= link_to "#{job_prospect.company}", job_prospect_path(job_prospect)%></td>
	<td><%= job_prospect.position%></td>
	<td><%= link_to "Delete", job_prospect_path(job_prospect), :method => :delete, :remote => true %>
</tr>
