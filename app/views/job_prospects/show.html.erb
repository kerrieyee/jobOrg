<h1><%=@job_prospect.company%></h1>
<h2><%=@job_prospect.position%></h2>

<%= button_to "Add Event", new_job_prospect_event_path(@job_prospect), :class => "btn btn-primary", :method => :get %>

<%= render 'shared/events_table'%>

<%= link_to "All #{@job_prospect.company} Events", job_prospect_events_path(@job_prospect)%> | 
<%= link_to "Return to all Job Prospects", job_prospects_path%>